apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ms-kafka  
  labels:
    app: postgres
spec:
  ports:
    - port: 5432
  selector:
    app: mskafka-postgres
    tier: db
  clusterIP: None
---
apiVersion: apps/v1beta2 # for versions before 1.8.0 use apps/v1beta1
kind: Deployment
metadata:
  name: mskafka-postgres
  namespace: ms-kafka  
  labels:
    app: mskafka-postgres
spec:
  selector:
    matchLabels:
      app: mskafka-postgres
      tier: db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mskafka-postgres
        tier: db
    spec:
      containers:
      - name: mskafka-postgres
        image: misegr/mskafka_postgres
        imagePullPolicy: IfNotPresent    
        env:
        - name: POSTGRES_PASSWORD
          value: dbpass
        - name: POSTGRES_USER
          value: dbuser
        ports:
        - containerPort: 5432
          name: postgres